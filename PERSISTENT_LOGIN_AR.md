# ميزة الاحتفاظ بتسجيل الدخول التلقائي
## Persistent Login Feature

---

## 📋 نظرة عامة

تم تفعيل ميزة **الاحتفاظ بتسجيل الدخول التلقائي** في نظام صرح الإتقان. الآن لن تحتاج لإدخال بيانات تسجيل الدخول في كل مرة تفتح فيها التطبيق. سيبقى المستخدم مسجلاً دخوله حتى يقوم بالضغط على زر "تسجيل الخروج" يدوياً.

---

## ✨ كيف تعمل الميزة

### 🔐 عند تسجيل الدخول لأول مرة:

1. المستخدم يختار البوابة (مسؤول/موظف)
2. يُدخل اسم المستخدم وكلمة المرور
3. بعد التحقق من البيانات، يتم:
   - حفظ بيانات المستخدم في `spark.kv` (تخزين دائم)
   - الانتقال تلقائياً للوحة التحكم المناسبة

### 🔄 عند فتح التطبيق مرة أخرى:

1. التطبيق يتحقق من وجود بيانات مستخدم محفوظة
2. إذا وُجدت بيانات محفوظة:
   - ✅ الانتقال **مباشرة** للوحة التحكم
   - ✅ عرض البيانات الخاصة بالمستخدم
   - ✅ **لا حاجة لإعادة تسجيل الدخول**
3. إذا لم توجد بيانات:
   - عرض شاشة اختيار البوابة

### 🚪 عند تسجيل الخروج:

1. المستخدم يضغط على زر "تسجيل الخروج"
2. يتم:
   - ❌ مسح جميع بيانات المستخدم من `spark.kv`
   - ❌ إعادة التوجيه لشاشة اختيار البوابة
   - ✅ عرض رسالة تأكيد تسجيل الخروج

---

## 🛠️ التفاصيل التقنية

### استخدام `useKV` Hook

```typescript
const [currentUser, setCurrentUser] = useKV<Employee | null>('currentUser', null)
```

- **المفتاح**: `'currentUser'` - يحفظ بيانات المستخدم الحالي
- **القيمة الافتراضية**: `null` - عند عدم وجود مستخدم مسجل
- **النوع**: `Employee | null` - إما بيانات موظف أو null

### استعادة الجلسة تلقائياً

```typescript
useEffect(() => {
  if (currentUser) {
    setAppView('dashboard')
    setSelectedPortal(currentUser.role)
  }
}, [])
```

- يتم تشغيل هذا الكود **مرة واحدة** عند تحميل التطبيق
- إذا وُجد `currentUser` محفوظ، يتم:
  - الانتقال للوحة التحكم `'dashboard'`
  - تحديد البوابة المناسبة حسب دور المستخدم

### تسجيل الخروج

```typescript
const handleLogout = () => {
  setCurrentUser(null)  // ✅ مسح البيانات من spark.kv
  setSelectedPortal(null)
  setAppView('portal-selection')
  setCurrentView('daily')
  toast.success(t('messages.logoutSuccess'))
}
```

- `setCurrentUser(null)` يقوم **تلقائياً** بمسح البيانات من التخزين الدائم
- يتم إعادة المستخدم لشاشة اختيار البوابة

---

## 🔒 الأمان

### ✅ الميزات الأمنية:

1. **تخزين آمن**: البيانات محفوظة في `spark.kv` (تخزين آمن ومعزول)
2. **تسجيل خروج كامل**: عند الضغط على "تسجيل الخروج" يتم مسح **جميع** البيانات
3. **عدم مشاركة الجلسات**: كل مستخدم له جلسة منفصلة
4. **صلاحيات محددة**: المسؤول والموظف لكل منهم صلاحيات مختلفة

### ⚠️ ملاحظات أمنية:

- لا تستخدم الجهاز في مكان عام دون تسجيل الخروج
- في حالة الأجهزة المشتركة، **تأكد من تسجيل الخروج** بعد الانتهاء
- بيانات تسجيل الدخول آمنة ولا يمكن الوصول إليها من تطبيقات أخرى

---

## 📱 تجربة المستخدم

### للمسؤول:

```
[فتح التطبيق] → [تسجيل دخول مرة واحدة] → [استخدام يومي دون إعادة تسجيل]
                                              ↓
                                    [لوحة تحكم المسؤول مباشرة]
```

### للموظف:

```
[فتح التطبيق] → [تسجيل دخول مرة واحدة] → [استخدام يومي دون إعادة تسجيل]
                                              ↓
                                    [لوحة تحكم الموظف مباشرة]
```

---

## 🎯 الفوائد

### ⚡ السرعة:
- فتح التطبيق والدخول مباشرة للوحة التحكم
- لا وقت ضائع في إعادة كتابة بيانات الدخول

### 🎨 تجربة أفضل:
- تجربة استخدام سلسة ومريحة
- مناسب للاستخدام اليومي المتكرر

### 📊 إنتاجية أعلى:
- الموظفون يمكنهم تسجيل الحضور بسرعة
- المسؤول يمكنه الدخول للنظام فوراً

---

## 🔄 سيناريوهات الاستخدام

### السيناريو 1: الاستخدام اليومي العادي

```
اليوم الأول:
1. فتح التطبيق
2. اختيار "بوابة الموظف"
3. إدخال اسم المستخدم: "abdullah"
4. إدخال كلمة المرور: "123"
5. تسجيل الدخول → الدخول للوحة التحكم

اليوم الثاني وما بعده:
1. فتح التطبيق
2. ✨ الدخول مباشرة للوحة التحكم (بدون خطوات إضافية)
```

### السيناريو 2: تغيير المستخدم

```
الموظف الحالي:
1. الضغط على "تسجيل الخروج"
2. العودة لشاشة اختيار البوابة

موظف آخر:
1. اختيار البوابة
2. إدخال بيانات تسجيل الدخول الخاصة به
3. الدخول للوحة التحكم الخاصة به
```

### السيناريو 3: جهاز مشترك

```
نهاية الدوام:
1. الضغط على "تسجيل الخروج" (⚠️ مهم جداً)
2. التأكد من ظهور شاشة اختيار البوابة
3. إغلاق المتصفح بأمان
```

---

## 🆘 الأسئلة الشائعة

### ❓ هل بياناتي آمنة؟
✅ نعم، البيانات محفوظة بشكل آمن في `spark.kv` ومعزولة عن التطبيقات الأخرى.

### ❓ ماذا لو نسيت تسجيل الخروج؟
⚠️ سيبقى المستخدم التالي قادراً على الدخول بحسابك. **يُنصح دائماً** بتسجيل الخروج.

### ❓ هل يمكنني تعطيل هذه الميزة؟
✅ نعم، ببساطة قم بتسجيل الخروج بعد كل استخدام. سيطلب منك التطبيق تسجيل الدخول في المرة القادمة.

### ❓ ماذا يحدث عند مسح بيانات المتصفح؟
⚠️ سيتم مسح جلسة تسجيل الدخول، وستحتاج لإعادة تسجيل الدخول.

### ❓ هل تعمل الميزة على الهاتف؟
✅ نعم، تعمل على جميع الأجهزة (هاتف، كمبيوتر، تابلت).

---

## 📝 ملاحظات للمطورين

### البنية التقنية:

```typescript
// State Management
const [currentUser, setCurrentUser] = useKV<Employee | null>('currentUser', null)

// Auto-restore session on mount
useEffect(() => {
  if (currentUser) {
    setAppView('dashboard')
    setSelectedPortal(currentUser.role)
  }
}, [])

// Logout clears persistent storage
const handleLogout = () => {
  setCurrentUser(null) // Automatically removes from spark.kv
  // ... reset other state
}

// Login saves to persistent storage
const handleLogin = (username: string, password: string) => {
  const employee = authenticateEmployee(username, password, safeEmployees)
  if (employee && employee.role === selectedPortal) {
    setCurrentUser(employee) // Automatically saves to spark.kv
    setAppView('dashboard')
  }
}
```

### البيانات المحفوظة:

```json
{
  "currentUser": {
    "id": 1,
    "name": "عبد الله",
    "nameHindi": "अब्दुल्लाह",
    "center": "صرح الاتقان الرئيسي",
    "time": "08:00",
    "salary": 4000,
    "position": "مدير",
    "username": "abdullah",
    "password": "123",
    "role": "admin"
  }
}
```

---

## 🎉 الخلاصة

تم تفعيل ميزة **الاحتفاظ بتسجيل الدخول التلقائي** بنجاح! الآن يمكن للمستخدمين:

✅ تسجيل الدخول مرة واحدة فقط  
✅ الدخول مباشرة للوحة التحكم في المرات التالية  
✅ تسجيل الخروج بسهولة عند الحاجة  
✅ استخدام آمن ومريح للتطبيق  

---

**تم التطوير بواسطة**: فريق صرح الإتقان التقني  
**التاريخ**: 2024  
**الإصدار**: 1.0.0
